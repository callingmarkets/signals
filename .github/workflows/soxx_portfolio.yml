name: Semiconductor Portfolio

on:
  workflow_run:
    workflows: ["Natural Resources Portfolio"]   # Runs after energy completes
    types: [completed]
  workflow_dispatch:

jobs:
  update-portfolio:
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install requests pandas numpy

      - name: Run Semiconductor Portfolio Engine
        env:
          TIINGO_API_KEY: ${{ secrets.TIINGO_API_KEY }}
        run: python soxx_portfolio.py

      - name: Commit portfolios.json
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add portfolios.json
          git diff --cached --quiet || git commit -m "chore: update semiconductor portfolio [skip ci]"
          git push
